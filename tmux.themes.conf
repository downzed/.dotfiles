## color palettes

## rose-pine
# set -g @MainBG '#191724'
# set -g @MainFG '#e0def4'

# set -g @SecondaryFG '#9ccfd8'
# set -g @SecondaryBG '#26233a'
# set -g @SecondaryFG2 '#f6c177'
# set -g @SecondaryBG2 '#403d52'

# set -g @Highlight '#eb6f92'
# set -g @Special '#31748f'

## gruvbox-dark-medium
# set -g @MainBG '#504945'  # Medium dark brown, soothing for the eyes
# set -g @MainFG '#ebdbb2'  # Light beige, excellent contrast for text against the darker background

# set -g @SecondaryFG '#b8bb26'  # Bright yellow-green, stands out for secondary information
# set -g @SecondaryBG '#665c54'  # Slightly lighter brown than MainBG, offers subtle differentiation
# set -g @SecondaryFG2 '#d3869b'  # Soft pink, provides a gentle highlight without overwhelming
# set -g @SecondaryBG2 '#3c3836'  # Dark gray, for less prominent yet important sections

# set -g @Highlight '#fb4934'  # Vibrant orange-red, extremely noticeable for important highlights
# set -g @Special '#8ec07c'    # Soft green, distinct yet understated for special cues

## gruvbox-light
# set -g @MainBG '#fbf1c7'  # Creamy light background for overall visibility
# set -g @MainFG '#3c3836'  # Dark gray for primary text, providing strong contrast

# set -g @SecondaryFG '#458588'  # Teal for secondary text, less prominent but readable
# set -g @SecondaryBG '#f2e5bc'  # Lighter beige, slightly different from MainBG for subtle distinction
# set -g @SecondaryFG2 '#d79921'  # Gold for highlighted items, draws attention without being too harsh
# set -g @SecondaryBG2 '#ebdbb2'  # Soft beige, maintains readability and consistency

# set -g @Highlight '#cc241d'  # Bright red for critical highlights, very eye-catching
# set -g @Special '#b16286'    # Muted purple for special markers or sections, offers a nice pop of color

## melange (dark)
# set -g @MainBG '#292522'                  # Dark brown background
# set -g @MainFG '#ECE1D7'                  # Light beige foreground for high contrast text

# set -g @SecondaryFG '#E49B5D'             # Orange for secondary foreground (from 'normal' yellow)
# set -g @SecondaryBG '#34302C'             # Very dark brown (almost black) for secondary background
# set -g @SecondaryFG2 '#EBC06D'            # Lighter yellow for alternative secondary foreground (from 'bright' yellow)
# set -g @SecondaryBG2 '#403d52'            # Dark slate gray for alternative secondary background

# set -g @Highlight '#BD8183'               # Soft red from 'normal' for highlights, good for attention without being too harsh
# set -g @Special '#7B9695'                 # Muted teal from 'normal' cyan for special elements, stands out yet fits the theme

## melange (darker?)
# set -g @MainBG '#1F1C1A'                  # Darker shade of brown for the main background
# set -g @MainFG '#F0E4D7'                  # Lighter beige for the foreground to increase contrast

# set -g @SecondaryFG '#E49B5D'             # Orange for secondary foreground, maintains original choice for continuity
# set -g @SecondaryBG '#2B2724'             # Darker brown for secondary background
# set -g @SecondaryFG2 '#EBBC7D'            # Brighter yellow for alternative secondary foreground, increasing contrast
# set -g @SecondaryBG2 '#363238'            # Darker grey for alternative secondary background

# set -g @Special '#C9626E'               # A darker, richer red for highlights to improve contrast and focus
# set -g @Highlight '#6D8B8D'                 # A deeper shade of teal for special elements, ensuring visibility against dark backgrounds

## monokai (pro? - chatgpt did it)
set -g @MainBG '#2d2a2e'      # black
set -g @MainFG '#fcfcfa'      # white

# Secondary interface elements
set -g @SecondaryFG  '#78dce8' # blue
set -g @SecondaryBG  '#727072' # gray
set -g @SecondaryFG2 '#ffd866' # yellow
set -g @SecondaryBG2 '#a9dc76' # green

# Highlight and special elements
set -g @Highlight '#ff6188'    # red
set -g @Special   '#fc9867'    # magenta
set -g @Special2  '#ab9df2'    # cyan
